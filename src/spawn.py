# TODO: create a function that spawns a terminal with server.py running
# This will allow for any room to become active one a user requests to
# either create one or join one. This probably makes no sense
# but in my head it does, so that's what counts

print("This will spawn a terminal with server.py running on a specific port\n");
